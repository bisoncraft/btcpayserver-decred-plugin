# Optional fragment to expose Decred RPC port for debugging.
# Place in: docker-compose-generator/docker-fragments/opt-decred-expose.yml

version: "3"

services:
  dcrwallet:
    ports:
      - "127.0.0.1:9110:9110"
required:
  - "decred"
